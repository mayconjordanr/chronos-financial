import { LoginForm } from '@/components/auth/login-form'

export const metadata = {
  title: 'Sign In | Chronos Financial',
  description: 'Sign in to your Chronos Financial account',
}

export default function LoginPage() {
  return <LoginForm />
}